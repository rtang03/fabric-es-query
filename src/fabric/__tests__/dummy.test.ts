import { logger } from '../../utils';
import { processBlockEvent } from '../processBlockEvent';
import { block0, block1, block2, block3, block4, block5, block6, block7, block8, block9, block10 } from './__generated__';

describe('dummy', () => {
  // it('test', async () => {
  //   const a = processBlockEvent(block0, logger);
  //   return true;
  // });
  // it('test', async () => {
  //   const a = processBlockEvent(block1, logger);
  //   return true;
  // });
  // it('test', async () => {
  //   const a = processBlockEvent(block2, logger);
  //   return true;
  // });
  // it('test', async () => {
  //   const a = processBlockEvent(block3, logger);
  //   return true;
  // });
  // it('test', async () => {
  //   const a = processBlockEvent(block4, logger);
  //   return true;
  // });
  // it('test', async () => {
  //   const a = processBlockEvent(block5, logger);
  //   return true;
  // });
  // it('test', async () => {
  //   const a = processBlockEvent(block6, logger);
  //   return true;
  // });
  it('test', async () => {
    const a = processBlockEvent(block7, logger);
    return true;
  });
  it('test', async () => {
    const a = processBlockEvent(block8, logger);
    return true;
  });
  it('test', async () => {
    const a = processBlockEvent(block9, logger);
    return true;
  });
  it('test', async () => {
    const a = processBlockEvent(block10, logger);
    return true;
  });
});
